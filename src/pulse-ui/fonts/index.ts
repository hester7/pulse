import { Inter, Tourney } from "next/font/google";

export const inter = Inter({
    subsets: ["latin"],
    display: "swap",
});

export const logo = Tourney({
    subsets: ["latin"],
    display: "swap",
});
